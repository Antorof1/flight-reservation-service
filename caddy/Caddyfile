{
	servers {
		trusted_proxies static private_ranges {$TRUSTED_PROXIES}
	}
}


{$DOMAIN} {
    import /etc/caddy/conf.d/*.caddy

    reverse_proxy app:8080
}

grafana.{$DOMAIN} {
    import /etc/caddy/conf.d/*.caddy

    reverse_proxy grafana:3000
}
